kafka: {}
kafka/full:
  max_message_bytes: 1
  required_acks: 0
  compression: gzip
  flush_max_messages: 2

# Invalid configurations
kafka/invalid_compression:
  compression: brotli
kafka/invalid_required_acks:
  required_acks: 3
